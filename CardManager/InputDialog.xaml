<Window x:Class="CardManager.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Category" 
        Height="200" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#3d3028"
        Icon="Resources/application.ico">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   x:Name="PromptText"
                   Text="Enter category for this card:"
                   Foreground="#e8d5b5"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>
        
        <ComboBox Grid.Row="1" 
                  x:Name="InputComboBox"
                  IsEditable="True"
                  Height="38"
                  FontSize="14"
                  Background="#2a2218"
                  Foreground="#1a1a1a"
                  BorderBrush="#5a4a3a"/>
        
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel" 
                    Background="#4a3d30"
                    Foreground="#d4c4a8"
                    BorderBrush="#6a5a4a"
                    BorderThickness="1"
                    Padding="16,10"
                    FontWeight="Medium"
                    Width="90"
                    Cursor="Hand"
                    Click="CancelButton_Click"/>
            <Button Content="OK" 
                    Background="#c49a4a"
                    Foreground="#1a1408"
                    BorderThickness="0"
                    Padding="16,10"
                    FontWeight="SemiBold"
                    Width="90"
                    Cursor="Hand"
                    Margin="8,0,0,0"
                    Click="OkButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
